package src.java;

import java.awt.Toolkit;

public class Consts {
    public final static int WINDOWWIDTH = 500;
    public final static int WINDOWHEIGHT = 600;

    public final static int FULLSCREENWIDTH = (int)(Toolkit.getDefaultToolkit().getScreenSize().getWidth());
    public final static int FULLSCREENHEIGHT = (int)(Toolkit.getDefaultToolkit().getScreenSize().getHeight());
}
